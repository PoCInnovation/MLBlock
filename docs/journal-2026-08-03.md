# Journal de développement — MLBlock (todo list)

> Mise à jour : 2026-08-07. Chaque partie liste les items réalisés `[x]` (avec commit/change OpenSpec) et ce qui reste à faire `[ ]`.

## Partie 1 — Éditeur & canvas

### Fait

- [x] **Mode avancé React Flow** (`react-flow-canvas`) : canvas zoom/pan/minimap, `BlockNode` custom (handles multi-ports + dtype au survol), palette drag & drop par catégorie, recherche + chips de filtre
- [x] **Navigation URL-based** (`react-router-navigation`) : React Router, 7 routes, `/editor` et `/projets` protégées (guard → `/login`), suppression du store `screen`
- [x] **Fix clignotement — controlled flow** (`14d009c`) : boucle de rendu infinie (1400 renders/s) causée par le double-sync canvas↔store → store = seule source de vérité (`applyFlowNodeChanges`/`addFlowNode`/`addFlowEdges`), zéro effet de sync
- [x] **Params éditables en mode avancé** (`advanced-mode-params`, `deacd65`) : `BlockMeta.coerce_params` (strings → types), `BlockNode` rend `BlockSegments` éditables, `data.fields` propagé au run
- [x] **Intelligence des params** (`block-param-intelligence`, `dbbfc86`) : métadonnées docstring FR (`(entre: 0-1, pas: 0.05)`, `(impair)`, `(choix:)`, `(format:)`), validation live feu tricolore, autocomplétion `target_column` dataflow-aware (`GET /api/files/columns`)
- [x] **Type checking & conversions — feu tricolore** (`type-checking-conversions`, `ee35410`) : dérivation des inputs (règle `in_<chiffres>` + types data), multi-sorties tuple, `core/types.py` (familles + classifieur 4 sorties), graphe de conversion `transforms`, validation réanimée, `df_to_tensor` créé, 5 blocs réparés — **archivée + specs syncées**
- [x] **Bouton suppression en avancé** (`51495d4`) : `removeFlowNode` + bouton × sur `BlockNode`
- [x] **Bouton « Pas à pas » supprimé** (`15f647e`) : placeholder mort, aucune logique câblée
- [x] **Retour à la ligne entre les params** (`b398c69`) : chaque param = rangée pleine largeur (`flexBasis: 100%`) en linéaire ET avancé
- [x] **Mode éditeur persisté** (`b4e0d19`) : `editorMode` dans localStorage — le refresh reste en mode avancé
- [x] **Fix divers** : drop cassait l'UI/zoom, labels de blocs, `Tout effacer` en avancé, edges snake_case (`source_port`), run envoyait les labels FR au lieu de `data.type`, react import manquant

### Reste à faire

- [ ] Animation « convoyeur de camions » sur les liens (transfert de données)
- [ ] Sélecteur multi-runs dans le panel Résultats (`GET /pipelines/{id}/jobs` existe déjà)

## Partie 2 — Auth

### Fait

- [x] **Session restaurée au refresh** (`auth-refresh-persistence`, `4be37c4`, archivée) : gate `authReady` dans `App.tsx` (splash avant les routes, fixe la race du guard) — le refresh sur `/editor` ne rebondit plus vers `/login`
- [x] **Enregistrement navigateur des identifiants** : `autoComplete` email/current-password (login), email/new-password ×2 (register) — aucun mot de passe stocké par l'app
- [x] **RHF + validation** (`react-hook-form`, `ea8fb74`) : Controller + famille `Field`/`FieldLabel`/`FieldError`, bordure rouge sur invalide, checklist mot de passe temps réel, erreurs FR (`mapSupabaseError`)
- [x] **OAuth Microsoft** (`supabase-oauth-providers`, `3ab64e5` — **non archivée**) : `signInWithMicrosoft()` (provider `azure`, scope `email`), bouton LoginPage, `docs/supabase-auth.md`
- [x] **Fix serveur local** : `load_dotenv` déplacé avant les imports dans `main.py` (auth.py lit l'env au module-level → serveur local ne pouvait authentifier personne)

### Reste à faire

- [ ] Archiver `supabase-oauth-providers` (7/7) + `ui-param-polish` (14/14) + `dict-port-splitting` (8/8) — 3 changes complétées non archivées
- [ ] Credentials Google Cloud + Azure Entra → dashboard Supabase (redirect `…/auth/v1/callback`) — action utilisateur

## Partie 3 — Projets (persistance)

### Fait

- [x] **Page « Mes projets »** (`projets-persistence`, `bfba00e`, archivée) : `/projets` — cartes (nom, date, nb blocs), Ouvrir / Exporter / Supprimer (confirm), + Nouveau (désactivé à 20), Importer, compteur « N/20 »
- [x] **Modèle draft** : run sans projet → brouillon invisible (`is_draft=true`, 1 seul/user, nettoyé au create) ; « Sauvegarder » formalise ; liste = non-drafts uniquement
- [x] **Limites & intégrité** : plafond 20 projets/user (409 FR), FK `pipelines.user_id → profiles ON DELETE CASCADE` (user → pipelines → jobs → job_outputs), cleanup CSV storage au delete
- [x] **Import/Export** : modal [JSON | Code] + téléchargement, import strict format MLBlock (validation forme client + types serveur via `PipelineDef`), symétrie round-trip, `position` du canvas conservée
- [x] **Nom du projet éditable inline** (`d8934e3`) : clic sur le nom dans l'éditeur → input inline (Enter valide / Escape annule), persisté **uniquement** au clic « 💾 Sauvegarder »
- [x] **Boutons « Mes projets »** (`70bcb8e`) : nav d'accueil (« Ouvrir l'éditeur » remplacé) + header éditeur
- [x] **Fix export pipeline vide** (`1cc3c8f`) : message FR « Ajoute des blocs au pipeline avant d'exporter le code » (pré-check + detail axios extrait)
- [x] **Migration** : `is_draft` + FK cascade + `chk_job_status` accepte `dispatched` (execute échouait toujours)

### Reste à faire

- [ ] Redéployer le backend sur Render (les commits depuis `1784006` : résultats, export fix…)

## Partie 4 — Résultats & exécution

### Fait

- [x] **Contrat de sortie typé** (`result-visualization`, `549495a` — prête à archiver) : `_serialize_output` dans le code généré — bytes → image (base64), liste → curve, dict `history` → curve, dict plat → metrics, scalaire → metric, sinon texte ; troncature `[:10000]` supprimée
- [x] **`plot_predictions` retourne les octets PNG** (`io.BytesIO`, `output_path` retiré)
- [x] **Panel « Résultats »** : tab à côté de Console — métriques clé/valeur, courbe SVG (sans lib), image data URL ; persisté (rechargé à l'ouverture d'un projet)
- [x] **Run de pipeline complet** : build (compile, accepte les pipelines non-tensor) → execute → polling job (3s) → outputs ; **exécution locale réelle** en subprocess (`MLBLOCK_RUN_MODE=local` ou clé Vast mock) — callbacks → `job_outputs`
- [x] **`GET /jobs/{id}/outputs`** (scopé user) ; **3 bugs pré-existants du code généré corrigés** : callbacks vers `/api/jobs/` (postaient `/jobs/` → 404), params coércés en littéraux (`ratio='0.8'` → `0.8`), `chk_job_status` DB

### Reste à faire

- [ ] Archiver `result-visualization`
- [ ] torchvision manquant dans `pyproject.toml` → blocs transforms plantent au runtime (décision en attente)

## Partie 5 — Déploiement & performance

### Fait

- [x] **Keep-alive** (`backend-keep-alive`, `36eb404`) : `GET /health` (sans DB), `docs/keep-alive.md`, UptimeRobot sur `/health`, arithmétique quota 720/750 h/mois ; GitHub Actions abandonné (testé, inopérant)
- [x] **Fix déploiement** (`fef9b4e`) : `pythonVersion: 3.11` dans render.yaml, matplotlib lazy dans `plot_predictions` (import 1,36 s), gymnasium/stable-baselines3 retirés, garde `isinstance(value, str)` dans coerce_params
- [x] **`fetchCatalog` retry auto** (5 × 15 s) + timeout axios 60 s (cold start free tier)
- [x] **`tsc --noEmit` au build frontend** (les erreurs TS bloquent le déploiement)

## Partie 6 — UI/UX global

### Fait

- [x] **Tout traduit en français** : 65 labels FR de blocs (`_fr_summary` exposé comme `description`, tooltips palette + nodes), erreurs FR
- [x] **Noms des outputs et params visibles** (`ui-param-polish`, `e07d959` — **non archivée**) : sorties `out_1 · dtype` sur les nodes, labels de params (`ratio:`), suggestions datalist, 62 labels FR, palette « Blocs »
- [x] **Cohérence palette** (`0d1f6f3`) : 3 tokens ajoutés (`inputBg`, `status`, `rail`), couleurs hors thème remplacées (login/register, editor, console, landing)
- [x] **Accessibilité** : focus-visible global, `<button>` au lieu de `<span onClick>`, contraste AA (textMuted), `role="alert"` sur les toasts

### Reste à faire

- [ ] Les tutos : deux cours (avancé / facile) au format .md → UI/UX → code
- [ ] Ajouter des types de données (images, …) et de nouveaux blocs (passer en revue les exercices de base de l'IA)
- [ ] Idée : toutes les sorties des blocs accessibles partout → système linéaire enrichi

## État des tests & environnement

- [x] Backend : **91 passed** (`test_server` 23+6 projets+2 résultats+1 import, `test_types` 22, `test_block` 9, `test_config`, `test_pipeline`…) — **7 échecs pré-existants** (`test_server`/`test_auth`, endpoints `/api/blocks*` supprimés), hors périmètre
- [x] Frontend : `tsc --noEmit` + `vite build` OK
- [x] Smokes navigateur : projets (créer → sauvegarder → liste → rouvrir → export/import round-trip → supprimer), résultats (run local sklearn → métrique + image dans le panel), refresh /editor (session + mode avancé conservés)
- [ ] Env : `.venv/bin/pytest` a un shebang vers un ancien venv → utiliser `uv run python -m pytest`
- [ ] Branches : `ali/dev` et `origin/frontend-backend-connection` à jour ; `main` = `dev/chedli` au dernier commit
