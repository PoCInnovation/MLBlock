def train_rl(env: "Environment", algorithm: "str" = 'PPO', total_timesteps: "int" = 100000, learning_rate: "float" = 0.0003, gamma: "float" = 0.99, policy: "str" = 'MlpPolicy', seed: "int" = None) -> "RLModel":
    """Entraîner un agent RL.
    
    Args:
        env: Input tensor.
        algorithm: Parameter.
        total_timesteps: Parameter.
        learning_rate: Parameter.
        gamma: Parameter.
        policy: Parameter.
        seed: Parameter.
    """
    pass
